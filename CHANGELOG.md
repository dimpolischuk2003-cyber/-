# Інтеграція з n8n і Google-таблицею

## Що додано

| Файл | Для чого |
|---|---|
| `n8n-workflow.json` | готовий воркфлоу — імпортувати в n8n |
| `n8n-headers.txt` | рядок заголовків для вставки в A1 таблиці |
| `N8N.md` | покрокова інструкція з розділом «якщо щось не працює» |
| `apps-script.gs` | альтернатива без n8n, якщо колись знадобиться |

## Як влаштований ланцюг

```
форма → /api/lead (Vercel) → webhook n8n → Google-таблиця
```

Воркфлоу з трьох нод: приймає заявку, перекладає технічні назви полів
у зрозумілі заголовки, записує рядок із пошуком за `lead_id`.

Ключовий момент — операція **append or update** з пошуком по `lead_id`.
Перший крок форми створює рядок, третій знаходить його і доповнює.
Один лід — один рядок.

## Що ще зроблено

**Колонка «Створено» не затирається.** На другому зверненні воркфлоу її не
надсилає взагалі, тому в таблиці лишається час першого контакту. Час останньої
зміни пишеться в окрему колонку «Оновлено».

**Захист вебхука.** Функція на Vercel тепер може додавати секретний заголовок
`X-Vseodent-Secret` зі змінної `LEAD_WEBHOOK_SECRET`. У n8n вмикається
Header Auth — і сторонні запити відсікаються. Без цієї змінної все працює
як раніше, заголовок просто не додається.

## Перевірено

- JSON воркфлоу валідний, три ноди, зв'язки на місці.
- Усі 28 ключів із Code-ноди збігаються із заголовками таблиці символ у символ.
- Жодне поле форми не загублене при перекладі в колонки.
- Помилок JS на сторінках немає.

## Що робите ви

1. Створюєте таблицю й вставляєте заголовки з `n8n-headers.txt` у A1.
2. Імпортуєте воркфлоу, підключаєте Google-акаунт, вставляєте ID таблиці.
3. Вмикаєте воркфлоу і копіюєте **Production URL** вебхука.
4. Кладете його у Vercel як `LEAD_WEBHOOK_URL` і робите **Redeploy**.

Деталі кожного кроку — у `N8N.md`.
