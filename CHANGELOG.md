# Правки: спікери, смуга на мобільному, ліди

## 3. Ліди не приходять — причина в назві аркуша

Це не помилка воркфлоу. На скриншоті видно, що дані дійшли до n8n
і розклались правильно: `lead_id`, курс, місто, ПІБ, Viber-група.
Ланцюг сайт → Vercel → n8n працює.

Падає остання нода:

```
Sheet with name Ліди VSEODENT not found
```

У полі **Sheet** вписано `Ліди VSEODENT`, а вкладка у вашій таблиці
називається інакше. Або впишіть у поле реальну назву вкладки,
або перейменуйте вкладку на `Ліди VSEODENT`. Символ у символ.

Заодно перевірте рядок 1 таблиці — там мають стояти заголовки
з `n8n-headers.txt`, інакше `Map Automatically` не знайде колонок.

## 1. Спікери вирівняні

Причина була у самих файлах. Три вирізані фото мали різні пропорції:
0.978, 0.759 і 0.784. У рамці 3:4 кожне масштабувалось по-своєму,
тому голови стояли на різній висоті, а нижній зріз фігури не доходив
до краю рамки.

Перебив усі три на спільне полотно 900×1200 (рівно 3:4): фігура
займає 90% висоти, притиснута до низу, по центру, зайве по боках
обрізане. Зверху лишається однакове повітря.

Тепер в CSS замість `contain` з відступами стоїть `cover` з прив'язкою
до низу — фото заповнює рамку без порожнеч і без зрізу. Виправлено
у трьох місцях: картки на головній, hero сторінки курсу, блок
«Спікер курсу».

## 2. Смуга розділів на мобільному

Повернув перший варіант: самі риски, назва показується тільки
в того блоку, на якому ви стоїте. Риски зменшені — 8px замість 14,
активна 14px замість 20. Прибрана напівпрозора підкладка, яка
перекривала контент.

На десктопі не змінював нічого.

## Перевірено
- Помилок JS і 404 немає на головній і всіх типах курсів.
- 375 / 390 / 430 px — горизонтального скролу немає.
