# Чому оновлення не було видно

## Причина — моя помилка в налаштуванні кешу

У `vercel.json`, який я віддав, стояло правило:

```
/assets/(.*)  →  Cache-Control: public, max-age=31536000, immutable
```

Це наказ браузеру: «кешуй усе з папки assets на рік і **ніколи не питай
сервер, чи не змінилось**». Я поставив його на самому початку, коли
в `assets` були лише картинки. Але потім туди ж лягли `data.js`
і `styles.css` — тобто весь контент і вся логіка сайту.

Через це браузер тримав стару копію `data.js`: з курсом Уляни
і зі старими шляхами до медіа. Нові відео лежали на сервері,
але старий `data.js` про них не знав.

Симптом «залив усе, а нічого не змінилось» — саме звідси.

## Що виправлено

**Правила кешу розділені за типом файлів:**

| Файли | Кеш |
|---|---|
| `assets/js`, `assets/css` | перепитувати сервер щоразу |
| `assets/images`, `assets/videos` | доба |
| html | перепитувати сервер щоразу |

**Додано версіонування.** Скрипти і стилі підключаються з міткою версії:
`data.js?v=d8ac2fcb`. Мітка — хеш від усього коду, вона змінюється сама
при кожній правці. Навіть якщо десь застрягне старий кеш, браузер
побачить нову адресу і завантажить свіжий файл.

Разом ці дві речі роблять повторення проблеми неможливим.

## Що зробити вам

1. Залийте оновлені файли (обов'язково `vercel.json`, `index.html`,
   `course.html` — у них зміни).
2. Redeploy на Vercel.
3. Відкрийте сайт і оновіть сторінку через `Ctrl+Shift+R`
   (Windows) або `Cmd+Shift+R` (Mac) — один раз, щоб скинути
   те, що браузер уже встиг закешувати на рік.

## Як перевірити, яка версія відкрита

Правою кнопкою по сторінці → «Переглянути код сторінки» → знайдіть
рядок `data.js?v=`. Має бути **d8ac2fcb**. Якщо там інша мітка
або її немає взагалі — відкритий старий деплой.

## Перевірено в цій збірці
- Спікерів двоє: Ноєнко і Федак. Уляни немає.
- Курсів 13.
- 16 відео і 16 фото по курсах на місці.
- Помилок JS і 404 немає.
