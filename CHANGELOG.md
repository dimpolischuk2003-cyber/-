# Правки №3 — що зроблено

## 1. Фільтр на головній
Фільтр за темами прибраний. Замість нього — помітний блок у рамці
із заголовком **«Оберіть ваше місто»** і великими кнопками-містами
(Усі міста + 7 міст). На мобільному кнопки переносяться в кілька рядів.

## 2. Ціна тільки у своєму блоці
Прибрана з hero сторінки курсу, з карток каталогу на головній,
з карток «інші міста» і з липкої панелі внизу. Залишилась лише
в блоці «Вартість участі».

## 3–4. Фотографії більше не обрізаються
Раніше фото кадрувалися через `object-fit: cover`. Тепер скрізь
`contain` — знімок видно повністю, він просто вписується в блок:

- картки спікерів на головній — пропорції 3:4
- hero сторінки курсу — 3:4 на десктопі, повна висота 260px на мобільному
- блок «Спікер курсу» — 3:4

## 5. Блок спікера
Велике фото ліворуч, поруч ім'я, спеціалізація, опис і **floating cards**
з фактами. При наведенні картка піднімається і підсвічується — без 3D,
лише micro-motion.

**Увага:** у документі після фрази «ось інформація про спікерів, яку
потрібно в такому форматі додати на сайт» тексту не було — він не дійшов.
Щоб блок не був порожнім, я склав картки **лише з того, що вже є в даних
сайту**: напрям роботи, формат курсів, підхід. Жодних регалій, стажу чи
цифр не вигадував. Замініть на ваші формулювання в `data.js` → `speakers` → `facts`.

## 6. Блок «Що зміниться у вашій роботі після курсу»
Стоїть одразу після блоку з цінами.

**Desktop:** одна картка, ліворуч сіра «ДО КУРСУ», праворуч синя
«ПІСЛЯ КУРСУ», між ними draggable-повзунок. Тягнете — колонка
розширюється. Межі 26–74%, щоб текст не ставав нечитабельним.
Працює мишею, пальцем і стрілками клавіатури.

**Mobile:** дві картки одна під одною. Повзунок прибраний свідомо —
у колонці шириною 170px текст із шести пунктів читати неможливо.
Підзаголовок на мобільному теж підмінюється, щоб не пропонувати
тягнути те, чого немає.

Тексти вставлені дослівно для всіх шести курсів і для головної,
разом з акцентними цитатами внизу.

## 7. Блок «Все необхідне перед реєстрацією» видалений
Бали БПР і № заходу МОЗ залишилися в hero, тож інформація не втрачена.

## 8. Відгуки
15 скриншотів додані. Розкладка в стилі masonry (3 колонки на десктопі,
2 на планшеті, 1 на мобільному) — скриншоти різної висоти лягають рівно.
Спочатку показуються 6, решта — за кнопкою «Показати всі відгуки (15)».

Оптимізація: PNG по 600–850 KB → WebP. Разом 15 файлів важать 671 KB
замість ~10 MB. Усі з `loading="lazy"`.

## 9. Друга кнопка в hero
Під «Забронювати місце» додана менш помітна **«Дізнатися ціну»** —
скролить до блоку вартості. Пояснення під кнопкою прибране.

## 10. Нові фото спікерів
Ігор Ноєнко і Уляна Тарасовська замінені в усіх курсах, де вони є.

Фото Уляни лежало на боці — це була EXIF-орієнтація, виправив.
Оригінал 5184×3456 (4 MB) → 1200×1800 WebP (49 KB).
Фото Ігоря 1707×2560 (626 KB) → 1200×1800 WebP (125 KB).

## 11. Смуга розділів
**Desktop:** збільшена — шрифт 16px замість 12px, риски вдвічі довші,
активна риска 56px. Щоб вона не налазила на контент, бокові поля
сторінки збільшені до 400px на екранах від 1280px.

**Mobile:** тепер видно **всі** назви розділів на напівпрозорій підкладці,
активний — яскравіший і більший. Це саме те, що ви просили, але майте
на увазі: на 390px смуга накриває правий край контенту. Підкладка
напівпрозора, тому текст під нею читається, проте якщо заважатиме —
скажіть, поверну варіант із назвою тільки активного розділу.

## Перевірено
- Горизонтального скролу немає на 375, 390, 430 px.
- Помилок JS і 404 немає на головній та на всіх 6 типах курсів.
- Повзунок «До/Після» протестований перетягуванням в обидва боки.
- Блок цін коректний на всіх форматах, включно з одинарною карткою 29.11.
